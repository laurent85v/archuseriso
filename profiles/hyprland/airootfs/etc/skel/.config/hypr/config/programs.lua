-- Programs -- default applications shared across the configuration.
-- Returned as a table so other modules can reuse these values.

return {
    term = "kitty",
    menu = "fuzzel",
    file_manager = "thunar",
    browser = "firefox",
    editor = "mousepad",
}
